name: github Action Workflow
on:
 push:
   branches:
     - main
jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Get secret
        run: echo "Not ..... ...... ${{secrets.SECRET_KEY}}"
      - name: Get Variable
        run: echo "Variable Value = ${{vars.VARIABLE_KEY}}"
